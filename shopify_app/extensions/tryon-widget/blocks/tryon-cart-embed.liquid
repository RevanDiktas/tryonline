{% comment %}
  TryOn — Cart listener app embed (optional if the Try On block is present).
  The Try On block now loads the same tryon-cart.js; use this embed only if you need
  the listener without the block, or keep both — the script binds once per page.
{% endcomment %}
{% schema %}
{
  "name": "TryOn cart",
  "target": "body",
  "javascript": "tryon-cart.js",
  "settings": []
}
{% endschema %}
